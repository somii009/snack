spring:
  output:
    ansi:
      enabled: always

  profiles:
    active: dev

  mvc:
    throw-exception-if-no-handler-found: true
    pathmatch:
      matching-strategy: ant_path_matcher
  web:
    resources:
      add-mappings: false